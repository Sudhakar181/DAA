#include <stdio.h>

int main() {
    int n = 10; // Example input size
    int i, j;

    for (i = 0; i < n; i++) {
        printf("O(n) - Iteration %d\n", i);
    }

   
    for (i = 0; i < n; i++) {
        for (j = 0; j < n; j++) {
            printf("O(n^2) - Iteration (%d, %d)\n", i, j);
        }
    }

    return 0;
}
